# duplicate-tab
A package for Pulsar Editor that adds a 'Duplicate Tab' option to the tabs right-click context menu.

## Usage
Right-click on any tab in the tabs bar and click `Duplicate Tab`.
Or press `Alt+Shift+D` while a pane is focused to duplicate currently active tab.
